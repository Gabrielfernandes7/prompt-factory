# HTML

O markup principal permanece em `index.html`, na raiz, para que o Prompt Factory continue abrindo diretamente no navegador. Este diretório fica reservado para futuros fragmentos HTML, caso eles sejam necessários.
